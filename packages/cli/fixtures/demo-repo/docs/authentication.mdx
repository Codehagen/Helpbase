---
schemaVersion: 1
title: "Authentication"
description: "How to authenticate requests to the API."
tags: ["auth", "api"]
order: 1
featured: true
---

## Sending authenticated requests

Include your API key in the `X-API-Token` header on every request:

```bash
curl https://api.example.com/v1/users \
  -H "X-API-Token: your_api_key_here"
```

Keys are scoped to a single environment. Rotate them from the dashboard
under **Settings → API keys**.
